{
  "name": "dies-webui-3d",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "wasm": "wasm-pack build ../crates/dies-simulator-wasm --target web",
    "dev": "npm run wasm && vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.2.0",
    "vite-plugin-wasm-pack": "^0.1.12"
  }
}
