# Pre-match checklist

Each `- [ ]` line below is confirmed one at a time in the terminal (press Enter).

- [ ] Correct team color and side confirmed with the referee
- [ ] Basestation ducktaped
- [ ] Field mask cleared and GC-ignore off (auto-forced — just confirm)
- [ ] ⚠️ Remember to DISABLE the `warmup` param in the Web UI before kickoff
