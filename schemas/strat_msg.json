{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "StratMsg",
  "description": "A message to a strategy process",
  "oneOf": [
    {
      "description": "Terminates the strategy process",
      "type": "object",
      "required": [
        "type"
      ],
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "Term"
          ]
        }
      }
    }
  ]
}