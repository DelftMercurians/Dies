[project]
name = "dies-mpc-codegen"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "sympy>=1.13",
    "numpy>=1.26",
    "matplotlib>=3.8",
    "jupyterlab>=4.0",
    "pytest>=8.0",
]

[tool.uv]
package = false
